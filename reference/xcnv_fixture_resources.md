# Locate the bundled deterministic fixture

Resolve the small, non-scientific fixture shipped for tests and
examples. It is not a substitute for the Zhang et al. annotations or
model.

## Usage

``` r
xcnv_fixture_resources()
```

## Value

An object of class `xcnv_resources`.

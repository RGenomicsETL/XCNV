# XCNV deterministic fixture

This directory is a tiny synthetic resource bundle used only by package tests
and examples. It is not the Zhang et al. annotation set, training data, or
XGBoost model and must not be used for scientific inference. Its portable tree
contains one synthetic split so the runtime evaluator can be tested without an
XGBoost installation.
